{
  "type": "module",
  "version": "0.0.2",
  "scripts": {
    "test": "clear && ava",
    "start": "npx bb-path-alias ./src/index.ts",
    "build": "clear && rm -rf ./dist && tsc --p tsconfig.build.json"
  },
  "devDependencies": {
    "@bleed-believer/path-alias": "^1.1.1",
    "@types/node": "^20.11.19",
    "ava": "^6.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@bleed-believer/mnt": "^0.0.3",
    "yaml": "^2.3.4"
  }
}
